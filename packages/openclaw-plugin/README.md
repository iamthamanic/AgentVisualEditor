# Agent Visual Editor (OpenClaw plugin)

Native composer chips and session-bound visual context. Selection never sends — only explicit Send.

```sh
npm install
npm run build
npm run validate
openclaw plugins install .
```

Enable **Settings → Labs → Custom plugin UI**, then select **AVE Composer mit Chips**.
