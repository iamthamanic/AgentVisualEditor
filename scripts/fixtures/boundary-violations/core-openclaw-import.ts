/**
 * Intentional OpenClaw SDK import — boundary fixture (must FAIL checker).
 * Location: scripts/fixtures/boundary-violations/core-openclaw-import.ts
 */
import { defineFeaturePlugin } from "openclaw/plugin-sdk/feature-plugin";

export const bad = defineFeaturePlugin;
