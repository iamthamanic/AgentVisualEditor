{
  "name": "agent-visual-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run build -w @agent-visual-editor/core -w @agent-visual-editor/protocol -w @agent-visual-editor/domscribe-adapter && npm run typecheck --workspaces --if-present",
    "build": "npm run build -w @agent-visual-editor/core -w @agent-visual-editor/protocol -w @agent-visual-editor/domscribe-adapter && npm run build --workspaces --if-present",
    "test": "npm run build -w @agent-visual-editor/core -w @agent-visual-editor/protocol -w @agent-visual-editor/domscribe-adapter && npm run test --workspaces --if-present && node --test scripts/test/**/*.test.mjs",
    "lint": "eslint .",
    "check:boundary": "node scripts/check-adapter-boundary.mjs",
    "check:licenses": "node scripts/check-license-notices.mjs",
    "setup:host": "node scripts/setup-host-toolchain.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.36.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.44.0"
  }
}
