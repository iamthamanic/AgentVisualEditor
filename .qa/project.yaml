name: agent-visual-editor
kind: monorepo
locale: de
appRoot: packages/openclaw-plugin
security:
  checklist: secure-by-default
typedStrict:
  languages:
    - typescript
testGate:
  profile: monorepo
  commands:
    - npm run typecheck
    - npm run build
    - npm test
    - npm run lint
    - npm run check:boundary
    - npm run check:licenses
    - cd packages/openclaw-plugin && npm run build && npm run validate
compositionGate:
  enabled: true
  notes: Prefer mountDefault composer composition; chip region above built-in;
    SLC-7 boundary is static (core/protocol host-free for future MCP)
pipeline:
  - implement
  - verify-ticket
  - composition-gate
  - review
